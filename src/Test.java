/*
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class Test {

    private BinaryTree tree;

    @BeforeEach
    public void setUp() {
        // Inicialize uma nova árvore antes de cada teste
        tree = new BinaryTree();
        // Construa a árvore com nós e valores conforme necessário
        // tree.insert(value);
        // tree.insert(anotherValue);
    }

    @Test
    public void testDeleteLeafNode() {
        // Execute o método delete em um nó folha
        tree.delete(leafNodeValue);

        // Verifique se o nó folha foi excluído corretamente
        assertNull(tree.search(leafNodeValue));
    }

    @Test
    public void testDeleteNodeWithOneChild() {
        // Execute o método delete em um nó com um único filho

        // Verifique se o nó com um único filho foi excluído corretamente
        assertNull(tree.search(nodeWithOneChildValue));
    }

    @Test
    public void testDeleteNodeWithTwoChildren() {
        // Execute o método delete em um nó com dois filhos

        // Verifique se o nó com dois filhos foi excluído corretamente
        assertNull(tree.search(nodeWithTwoChildrenValue));
    }

    @Test
    public void testDeleteRootNode() {
        // Execute o método delete na raiz da árvore

        // Verifique se a raiz da árvore foi excluída corretamente
        assertNull(tree.getRoot());
    }
}
*/